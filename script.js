// Form submission confirmation (optional)
document.querySelector('form').addEventListener('submit', function (e) {
  // Netlify handles submission, but add client-side alert
  alert('Thanks for signing up! Check your email.');
});
